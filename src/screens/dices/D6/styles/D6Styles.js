import { StyleSheet } from "react-native";

export const D6Styles = StyleSheet.create ({
    button: {
        width: '90%',
        aspectRatio: 1, 
        alignItems: 'center',
        justifyContent: 'center',
    },
    image: {
        width: '100%',
        height: '100%',
    }
});