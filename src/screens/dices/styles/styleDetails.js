import { StyleSheet } from "react-native";

export const styleDetails = StyleSheet.create ({
    title: {
        position: "absolute",
		transform: [{ scale: 0.7 }],
		alignSelf: 'center',
		width: '60%',
		height: '6%'
	}
});
