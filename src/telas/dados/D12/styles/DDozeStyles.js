import { StyleSheet } from "react-native";

export const DDozeStyles = StyleSheet.create({
	button: {
		overflow: 'hidden',
		position: 'absolute',
		height: '26.5%',
		width: '51%',
		top: '5%',
		alignSelf: 'center',
		right: '0%',
		transform: [{ scale: 0.85 }],
	}
});
